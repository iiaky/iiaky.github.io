class SubmissionMenu {
    constructor( {caster, enemy, onComplete} ) {
        this.caster = caster;
        this.enemey = enemy;
        this.onComplete = onComplete;
    }

    init(container) {

    }
}